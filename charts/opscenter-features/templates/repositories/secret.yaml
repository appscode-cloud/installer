{{- range $name, $data := .Values.repositoryCredentials }}
apiVersion: v1
kind: Secret
metadata:
  name: {{ include "opscenter-features.fullname" $ }}-{{ $name }}
  namespace: {{ $.Release.Namespace }}
stringData: {{ $data | toJson }}
---
{{- end }}
