apiVersion: source.toolkit.fluxcd.io/v1beta2
kind: HelmRepository
metadata:
  name: bytebuilders-ui
  namespace: {{ .Release.Namespace }}
spec:
{{- with (index .Values "repositories" "bytebuilders-ui") }}
  {{- with .url }}
  url: {{ . }}
  {{- end }}

  {{- with .interval }}
  interval: {{ . }}
  {{- end }}

  {{- with .type }}
  type: {{ . }}
  {{- end }}

  {{- with .provider }}
  provider: {{ . }}
  {{- end }}

  {{- with .secretName }}
  secretRef:
    name: {{ include "opscenter-features.fullname" $ }}-{{ . }}
  {{- end }}
{{- end }}
