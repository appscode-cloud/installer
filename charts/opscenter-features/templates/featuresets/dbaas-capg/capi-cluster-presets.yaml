{{- if eq .Values.capi.provider "capg" }}

apiVersion: ui.k8s.appscode.com/v1alpha1
kind: Feature
metadata:
  name: capi-cluster-presets
  labels:
    byte.builders/feature-set: dbaas-capg
spec:
  title: CAPI Cluster Presets
  description: |
    CAPI Cluster Presets
  icons:
    - src: https://cdn.appscode.com/k8s/icons/menu/cluster.svg
      type: image/svg+xml
  featureSet: dbaas-capg
  featureBlock: capi-cluster-presets
  recommended: true
  chart:
    name: capi-cluster-presets
    namespace: capi-cluster
    version: "v2023.10.18"
    sourceRef:
      kind: HelmRepository
      name: {{ ternary .Values.helm.release.channel "appscode-charts-oci" (has .Values.helm.release.channel (list "stable" "testing")) }}
      namespace: {{ .Release.Namespace }}
  values:
    capi:
      provider: {{ .Values.capi.provider }}
      namespace: {{ .Values.capi.namespace }}
      clusterName: {{ .Values.capi.clusterName }}

{{- end }}
