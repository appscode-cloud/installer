properties:
  capi:
    properties:
      clusterName:
        type: string
      namespace:
        type: string
      provider:
        enum:
        - capa
        - capg
        - capz
        type: string
    required:
    - clusterName
    - namespace
    type: object
  clusterManagers:
    items:
      type: string
    type: array
  fullnameOverride:
    type: string
  nameOverride:
    type: string
  registry:
    properties:
      credentials:
        additionalProperties:
          type: string
        type: object
    required:
    - credentials
    type: object
  release:
    properties:
      channel:
        default: dev
        enum:
        - stable
        - testing
        - dev
        type: string
    required:
    - channel
    type: object
  repositories:
    additionalProperties:
      properties:
        interval:
          pattern: ^([0-9]+(\.[0-9]+)?(ms|s|m|h))+$
          type: string
        provider:
          default: generic
          enum:
          - generic
          - aws
          - azure
          - gcp
          type: string
        secretName:
          type: string
        timeout:
          type: string
        type:
          enum:
          - default
          - oci
          type: string
        url:
          type: string
      required:
      - interval
      - url
      type: object
    type: object
required:
- capi
- clusterManagers
- fullnameOverride
- nameOverride
- registry
- release
- repositories
type: object
