{{- if eq .Values.tool "KubeStash" }}

apiVersion: v1
kind: Secret
metadata:
  name: default-encryption-secret
  namespace: {{ .Release.Namespace }}
stringData:
  key: {{ .Values.kubestash.encryptionSecret | quote }}

{{- end }}
