{{ $m := .Values.kubestash }}
{{- if .Values.global }}
{{- $m = .Values.global.kubestash }}
{{- end }}

apiVersion: charts.x-helm.dev/v1alpha1
kind: ClusterChartPreset
metadata:
  name: kubestash-presets
  labels:
    charts.x-helm.dev/is-default-preset: "true"
spec:
  values:
    spec:
      backup:
        tool: KubeStash
        {{- toYaml .Values | nindent 8 }}
