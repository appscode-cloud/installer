properties:
  capi:
    properties:
      clusterName:
        type: string
      namespace:
        type: string
      provider:
        enum:
        - capa
        - capg
        - capz
        type: string
    required:
    - clusterName
    - namespace
    type: object
required:
- capi
type: object
