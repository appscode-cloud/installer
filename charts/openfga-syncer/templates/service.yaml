apiVersion: v1
kind: Service
metadata:
  name: {{ include "openfga-syncer.fullname" . }}
  labels:
    {{- include "openfga-syncer.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "openfga-syncer.selectorLabels" . | nindent 4 }}
