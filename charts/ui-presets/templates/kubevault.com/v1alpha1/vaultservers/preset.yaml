apiVersion: charts.x-helm.dev/v1alpha1
kind: ClusterChartPreset
metadata:
  name: kubevault.com-v1alpha1-vaultservers
spec:
  selector: {}
  values:
  {{- with (dig "global" "monitoring" "serviceMonitor" "labels" .Values.monitoring.serviceMonitor.labels (dict "global" .Values.global)) }}
    monitoring:
      serviceMonitor:
        labels:
          {{- toYaml . | nindent 10 }}
  {{- end }}
  {{- with (dig "global" "monitoring" "alert" "labels" .Values.form.alert.labels (dict "global" .Values.global)) }}
    form:
      alert:
        labels:
          {{- toYaml . | nindent 10 }}
  {{- end }}
