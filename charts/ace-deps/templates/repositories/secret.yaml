{{- range $name, $data := .Values.repositoryCredentials }}
apiVersion: v1
kind: Secret
metadata:
  name: {{ include "ace-deps.fullname" $ }}-{{ $name }}
  namespace: {{ $.Release.Namespace }}
stringData: {{ $data | toJson }}
---
{{- end }}
