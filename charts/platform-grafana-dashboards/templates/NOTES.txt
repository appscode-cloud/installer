To verify that Platform Grafana Dashboards have been installed, run:

  kubectl get grafanadashboards --namespace {{ .Release.Namespace }} -l "app.kubernetes.io/name={{ include "platform-grafana-dashboards.name" . }},app.kubernetes.io/instance={{ .Release.Name }}"
