{{- range $name, $data := .Values.repositoryCredentials }}
apiVersion: v1
kind: Secret
metadata:
  name: {{ include "ace-installer.fullname" $ }}-{{ $name }}
  namespace: {{ $.Release.Namespace }}
stringData: {{ $data | toJson }}
---
{{- end }}
