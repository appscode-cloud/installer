# if basic auth or bearer token is provided, then create a secret for auth
{{- if or .Values.grafana.basicAuth.password .Values.grafana.bearerToken }}
apiVersion: v1
kind: Secret
metadata:
  name: {{ include "opscenter-config.fullname" . }}-grafana-auth
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "opscenter-config.labels" . | nindent 4 }}
stringData:
{{- with .Values.grafana.basicAuth.username }}
  username: {{ . | quote }}
{{- end }}
{{- with .Values.grafana.basicAuth.password }}
  password: {{ . | quote }}
{{- end }}
{{- with .Values.grafana.bearerToken }}
  token: {{ . | quote }}
{{- end }}
{{- end }}
