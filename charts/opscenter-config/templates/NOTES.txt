To verify that ByteBuilders Opscenter Configuration is setup, run:

  kubectl get appbinding --namespace {{ .Release.Namespace }} -l app.kubernetes.io/name={{ include "opscenter-config.name" . }},app.kubernetes.io/instance={{ .Release.Name }}
