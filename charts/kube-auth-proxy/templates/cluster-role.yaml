apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
  name: {{ include "kube-auth-proxy.fullname" . }}
  labels:
    {{- include "kube-auth-proxy.labels" . | nindent 4 }}
rules:
- apiGroups:
  - authentication.appscode.com
  resources: ["*"]
  verbs: ["get", "list", "watch"]
