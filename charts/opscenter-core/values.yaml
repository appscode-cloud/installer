# Default values for opscenter-core.
# This is a YAML-formatted file.
# Declare variables to be passed into your templates.

kube-ui-server:
  enabled: true
panopticon:
  enabled: true
grafana-operator:
  enabled: true
grafana-ui-server:
  enabled: true
kube-grafana-dashboards:
  enabled: true
operator:
  tag: v2022.03.20
