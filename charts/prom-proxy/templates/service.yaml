apiVersion: v1
kind: Service
metadata:
  name: {{ include "prom-proxy.fullname" . }}
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "prom-proxy.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "prom-proxy.selectorLabels" . | nindent 4 }}
