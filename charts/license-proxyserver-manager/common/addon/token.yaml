apiVersion: v1
kind: Secret
metadata:
  name: license-issuer-token
  namespace: {{ ternary "open-cluster-management-license-proxyserver" .Release.Namespace (empty .Values.kubeconfigSecretName) }}
type: Opaque
stringData:
  token: {{ .Values.platform.token | quote }}
