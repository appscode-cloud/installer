{{- if not (eq .Values.global.infra.dns.provider "external") }}
apiVersion: v1
kind: Secret
metadata:
  name: {{ include "ace.fullname" . }}-dns-cred
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "ace.labels" . | nindent 4 }}
type: Opaque
stringData:
{{- if eq .Values.global.infra.dns.provider "cloudflare" }}
  api-token: '{{ .Values.global.infra.dns.auth.token }}'
{{- end }}
{{- if eq .Values.global.infra.dns.provider "route53" }}
  AWS_ACCESS_KEY_ID: '{{ .Values.global.infra.dns.auth.AWS_ACCESS_KEY_ID }}'
  AWS_SECRET_ACCESS_KEY: '{{ .Values.global.infra.dns.auth.AWS_SECRET_ACCESS_KEY }}'
  AWS_REGION: '{{ .Values.global.infra.dns.auth.AWS_REGION }}'
{{- end }}
{{- end }}