{{ $objstore := .Values.global.infra.objstore }}

apiVersion: stash.appscode.com/v1alpha1
kind: Repository
metadata:
  name: {{ include "ace.fullname" . }}-db-repo
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "ace.labels" . | nindent 4 }}
spec:
  backend:
    storageSecretName: {{ include "ace.fullname" . }}-repo-cred

  {{- if eq $objstore.provider "s3" }}
    {{- with .Values.global.infra.stash.s3 }}
    s3:
      endpoint: {{ .endpoint }}
      bucket: {{ .bucket }}
      {{- with .region }}
      region: {{ . }}
      {{- end }}
      prefix: {{ .prefix }}
    {{- end }}
  {{- end }}

  {{- if eq $objstore.provider "azure" }}
    {{- with .Values.global.infra.stash.azure }}
    azure:
      container: {{ .container }}
      prefix: {{ .prefix }}
      {{- with .maxConnections }}
      maxConnections: {{ . }}
      {{- end }}
    {{- end }}
  {{- end }}

  {{- if eq $objstore.provider "gcs" }}
    {{- with .Values.global.infra.stash.gcs }}
    gcs:
      bucket: {{ .bucket }}
      prefix: {{ .prefix }}
      {{- with .maxConnections }}
      maxConnections: {{ . }}
      {{- end }}
    {{- end }}
  {{- end }}

  {{- if eq $objstore.provider "swift" }}
    {{- with .Values.global.infra.stash.swift }}
    swift:
      container: {{ .container }}
      prefix: {{ .prefix }}
    {{- end }}
  {{- end }}
