apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: inbox-ui
  namespace: argocd
spec:
  project: default
  source:
    chart: inbox-ui
    repoURL: ghcr.io/appscode-charts
    targetRevision: v2026.3.30
    helm:
      values: |
        ace-user-roles:
          enabled: false
  destination:
    server: "https://kubernetes.default.svc"
    namespace: ace
  syncPolicy:
    automated: {}
    syncOptions:
    - CreateNamespace=true
